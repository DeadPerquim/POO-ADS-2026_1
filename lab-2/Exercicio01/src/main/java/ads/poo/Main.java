package ads.poo;

import java.lang.classfile.constantpool.IntegerEntry;

public class Main {
    static void main(){
        int dia = Integer.parseInt(IO.readln("Entre com o dia: "));
        IO.println("dia: " + dia);

        String mes = IO.readln("Entre com o mês: ");
        IO.println("mês: " + mes);

        // TODO aprender a relacionar o dia com o mês
        if (dia.equals(""))
    }
}
